from __future__ import annotations

from datetime import datetime
from pathlib import Path
import py_compile
import shutil

PROJECT = Path(r"C:\Users\PC\projet")
TARGET = PROJECT / "src" / "gui" / "views" / "document_view.py"

NEW_SHOW = '    def show(self) -> None:\n        self.pages = self._load_project_pages()\n\n        # BANDEAU_NAVIGATION_PERMANENT_V1\n        # V6 : le nom de la page est désormais intégré au bandeau décoratif.\n        # L\'ancien en-tête séparé n\'est plus affiché, ce qui libère de la\n        # hauteur utile pour le contenu du Centre.\n        root = ctk.CTkFrame(\n            self.parent,\n            fg_color=self.WINDOW_BG,\n            corner_radius=0,\n        )\n        root.grid_columnconfigure(0, weight=1)\n        root.grid_rowconfigure(2, weight=1)\n\n        navigation = self._create_internal_navigation_ribbon(root)\n        navigation.grid(\n            row=0,\n            column=0,\n            sticky="ew",\n            padx=10,\n            pady=(5, 3),\n        )\n\n        workspace_bar = self._create_workspace_bar(root)\n        workspace_bar.grid(\n            row=1,\n            column=0,\n            sticky="ew",\n            padx=10,\n            pady=(0, 5),\n        )\n\n        main_workspace = self._create_main_workspace(root)\n        main_workspace.grid(\n            row=2,\n            column=0,\n            sticky="nsew",\n            padx=10,\n            pady=(0, 8),\n        )\n\n        root.update_idletasks()\n        root.pack(fill="both", expand=True)\n        root.lift()\n'
NEW_NAV = '    def _create_internal_navigation_ribbon(\n        self,\n        parent,\n    ) -> ctk.CTkFrame:\n        """Bandeau décoratif permanent de navigation PageMaître.\n\n        V6 :\n        - aucun fond de widget derrière les icônes ;\n        - tout est dessiné directement sur un seul Canvas ;\n        - le décor PageMaître est donc visible sous les icônes ;\n        - Accueil précède Centre dans le parcours ;\n        - le nom de la page est intégré au ruban.\n        """\n        ribbon = ctk.CTkFrame(\n            parent,\n            height=98,\n            fg_color="transparent",\n            corner_radius=0,\n            border_width=0,\n        )\n        ribbon.grid_propagate(False)\n        ribbon.grid_columnconfigure(0, weight=1)\n        ribbon.grid_rowconfigure(0, weight=1)\n\n        canvas = tk.Canvas(\n            ribbon,\n            background=self.WINDOW_BG,\n            borderwidth=0,\n            highlightthickness=0,\n            takefocus=False,\n            cursor="arrow",\n        )\n        canvas.grid(\n            row=0,\n            column=0,\n            sticky="nsew",\n        )\n\n        ribbon._nav_background_photo = None\n        ribbon._nav_click_regions = []\n\n        background_path = (\n            Path(__file__).resolve().parents[3]\n            / "assets"\n            / "interface"\n            / "backgrounds"\n            / "editorial_bg_accueil.png"\n        )\n\n        def hex_to_rgb(value: str) -> tuple[int, int, int]:\n            value = value.lstrip("#")\n            return tuple(\n                int(value[index:index + 2], 16)\n                for index in (0, 2, 4)\n            )\n\n        def draw_icon(\n            kind: str,\n            cx: float,\n            cy: float,\n            color: str,\n        ) -> None:\n            """Dessine une icône directement sur le décor, sans fond."""\n            stroke = color\n            soft = self._blend_hex(color, "#FFFFFF", 0.35)\n            w = 2\n\n            if kind == "door":\n                # Accueil : porte + flèche de retour.\n                canvas.create_rectangle(\n                    cx - 7, cy - 10,\n                    cx + 6, cy + 10,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx - 14, cy,\n                    cx - 4, cy,\n                    fill=stroke,\n                    width=w,\n                    arrow=tk.LAST,\n                    arrowshape=(7, 8, 3),\n                )\n                canvas.create_oval(\n                    cx + 2, cy - 1,\n                    cx + 4, cy + 1,\n                    fill=stroke,\n                    outline=stroke,\n                )\n\n            elif kind == "eye":\n                canvas.create_arc(\n                    cx - 15, cy - 8,\n                    cx + 15, cy + 8,\n                    start=200,\n                    extent=140,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_arc(\n                    cx - 15, cy - 8,\n                    cx + 15, cy + 8,\n                    start=20,\n                    extent=140,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_oval(\n                    cx - 5, cy - 5,\n                    cx + 5, cy + 5,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_oval(\n                    cx - 1.5, cy - 1.5,\n                    cx + 1.5, cy + 1.5,\n                    fill=stroke,\n                    outline=stroke,\n                )\n\n            elif kind == "sprout":\n                canvas.create_line(\n                    cx, cy + 11,\n                    cx, cy - 3,\n                    fill=stroke,\n                    width=w,\n                )\n                canvas.create_arc(\n                    cx - 12, cy - 10,\n                    cx, cy + 1,\n                    start=180,\n                    extent=170,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx - 9, cy - 7,\n                    cx, cy - 1,\n                    fill=soft,\n                    width=1,\n                )\n                canvas.create_arc(\n                    cx, cy - 12,\n                    cx + 12, cy,\n                    start=10,\n                    extent=170,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx, cy - 2,\n                    cx + 9, cy - 8,\n                    fill=soft,\n                    width=1,\n                )\n\n            elif kind == "home":\n                # Centre : maison, comme dans la proposition visuelle validée.\n                canvas.create_line(\n                    cx - 11, cy - 1,\n                    cx, cy - 11,\n                    cx + 11, cy - 1,\n                    fill=stroke,\n                    width=w,\n                    joinstyle=tk.ROUND,\n                )\n                canvas.create_rectangle(\n                    cx - 8, cy - 1,\n                    cx + 8, cy + 11,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_rectangle(\n                    cx - 2.5, cy + 4,\n                    cx + 2.5, cy + 11,\n                    outline=soft,\n                    width=1,\n                )\n\n            elif kind == "pencil":\n                canvas.create_line(\n                    cx - 9, cy + 9,\n                    cx + 7, cy - 7,\n                    fill=stroke,\n                    width=3,\n                )\n                canvas.create_line(\n                    cx + 5, cy - 9,\n                    cx + 10, cy - 4,\n                    fill=stroke,\n                    width=2,\n                )\n                canvas.create_polygon(\n                    cx - 11, cy + 11,\n                    cx - 7, cy + 9,\n                    cx - 9, cy + 7,\n                    fill="",\n                    outline=stroke,\n                    width=1,\n                )\n\n            elif kind == "tools":\n                canvas.create_line(\n                    cx - 9, cy + 9,\n                    cx + 8, cy - 8,\n                    fill=stroke,\n                    width=3,\n                )\n                canvas.create_line(\n                    cx - 8, cy - 8,\n                    cx + 9, cy + 9,\n                    fill=stroke,\n                    width=3,\n                )\n                canvas.create_rectangle(\n                    cx - 12, cy + 7,\n                    cx - 7, cy + 12,\n                    outline=stroke,\n                    width=1,\n                )\n                canvas.create_arc(\n                    cx + 5, cy - 12,\n                    cx + 12, cy - 5,\n                    start=20,\n                    extent=190,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=2,\n                )\n\n            elif kind == "quill":\n                canvas.create_arc(\n                    cx - 8, cy - 12,\n                    cx + 11, cy + 8,\n                    start=120,\n                    extent=190,\n                    style=tk.ARC,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx - 9, cy + 11,\n                    cx + 7, cy - 7,\n                    fill=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx - 2, cy + 4,\n                    cx + 6, cy + 3,\n                    fill=soft,\n                    width=1,\n                )\n                canvas.create_line(\n                    cx + 2, cy,\n                    cx + 8, cy - 2,\n                    fill=soft,\n                    width=1,\n                )\n\n            elif kind == "puzzle":\n                points = (\n                    cx - 10, cy - 8,\n                    cx - 3, cy - 8,\n                    cx - 3, cy - 11,\n                    cx, cy - 13,\n                    cx + 3, cy - 11,\n                    cx + 3, cy - 8,\n                    cx + 10, cy - 8,\n                    cx + 10, cy - 1,\n                    cx + 7, cy - 1,\n                    cx + 5, cy + 2,\n                    cx + 7, cy + 5,\n                    cx + 10, cy + 5,\n                    cx + 10, cy + 10,\n                    cx - 10, cy + 10,\n                    cx - 10, cy + 3,\n                    cx - 7, cy + 3,\n                    cx - 5, cy,\n                    cx - 7, cy - 3,\n                    cx - 10, cy - 3,\n                )\n                canvas.create_line(\n                    *points,\n                    cx - 10, cy - 8,\n                    fill=stroke,\n                    width=w,\n                    joinstyle=tk.ROUND,\n                )\n\n            elif kind == "verify":\n                canvas.create_oval(\n                    cx - 11, cy - 11,\n                    cx + 5, cy + 5,\n                    outline=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx + 3, cy + 3,\n                    cx + 12, cy + 12,\n                    fill=stroke,\n                    width=3,\n                )\n                canvas.create_line(\n                    cx - 7, cy - 2,\n                    cx - 3, cy + 2,\n                    cx + 2, cy - 5,\n                    fill=soft,\n                    width=2,\n                )\n\n            elif kind == "flag":\n                canvas.create_line(\n                    cx - 7, cy - 12,\n                    cx - 7, cy + 12,\n                    fill=stroke,\n                    width=w,\n                )\n                canvas.create_line(\n                    cx - 7, cy - 10,\n                    cx + 7, cy - 8,\n                    cx + 3, cy - 3,\n                    cx + 8, cy + 1,\n                    cx - 7, cy,\n                    fill=stroke,\n                    width=w,\n                    joinstyle=tk.ROUND,\n                )\n\n            elif kind == "close":\n                canvas.create_line(\n                    cx - 8, cy - 8,\n                    cx + 8, cy + 8,\n                    fill=stroke,\n                    width=3,\n                )\n                canvas.create_line(\n                    cx + 8, cy - 8,\n                    cx - 8, cy + 8,\n                    fill=stroke,\n                    width=3,\n                )\n\n        def build_background(width: int, height: int):\n            try:\n                from PIL import Image, ImageDraw\n            except Exception:\n                return None\n\n            if background_path.is_file():\n                try:\n                    source = Image.open(background_path).convert("RGBA")\n\n                    # Important : on garde tout le dessin du fond général.\n                    # On le comprime en hauteur au lieu de recadrer sa zone\n                    # centrale vide ; les références éditoriales restent donc\n                    # visibles aux extrémités du ruban.\n                    source = source.resize(\n                        (width, height),\n                        Image.Resampling.LANCZOS,\n                    )\n\n                    veil = Image.new(\n                        "RGBA",\n                        (width, height),\n                        (255, 255, 255, 105),\n                    )\n                    image = Image.alpha_composite(source, veil)\n\n                except Exception:\n                    image = Image.new(\n                        "RGBA",\n                        (width, height),\n                        (248, 247, 244, 255),\n                    )\n            else:\n                image = Image.new(\n                    "RGBA",\n                    (width, height),\n                    (248, 247, 244, 255),\n                )\n\n            # Quelques repères supplémentaires, strictement rectilignes.\n            draw = ImageDraw.Draw(image)\n            pale_blue = (117, 182, 219, 78)\n            pale_teal = (130, 183, 161, 72)\n            pale_lilac = (169, 151, 201, 66)\n            pale_coral = (223, 128, 107, 65)\n\n            y = height - 7\n            draw.line(\n                (18, y, width - 18, y),\n                fill=(77, 96, 118, 48),\n                width=1,\n            )\n\n            for x in (26, width // 2, width - 26):\n                draw.line(\n                    (x - 7, y, x + 7, y),\n                    fill=pale_blue,\n                    width=1,\n                )\n                draw.line(\n                    (x, y - 7, x, y + 1),\n                    fill=pale_blue,\n                    width=1,\n                )\n\n            sx = 214\n            for color in (\n                pale_teal,\n                pale_blue,\n                pale_lilac,\n                pale_coral,\n            ):\n                draw.rectangle(\n                    (sx, y - 4, sx + 9, y),\n                    outline=color,\n                    width=1,\n                )\n                sx += 14\n\n            return image\n\n        def add_click_region(\n            x1: float,\n            x2: float,\n            command,\n            enabled: bool,\n        ) -> None:\n            if enabled and callable(command):\n                ribbon._nav_click_regions.append(\n                    (float(x1), float(x2), command)\n                )\n\n        def on_click(event) -> None:\n            x = float(event.x)\n            for x1, x2, command in ribbon._nav_click_regions:\n                if x1 <= x <= x2:\n                    command()\n                    return\n\n        canvas.bind("<Button-1>", on_click)\n\n        def draw_item(\n            *,\n            cx: float,\n            icon_kind: str,\n            label: str,\n            color: str,\n            command=None,\n            active: bool = False,\n            enabled: bool = True,\n            width: float = 86,\n        ) -> None:\n            item_color = color if (enabled or active) else self.TEXT_LIGHT\n\n            draw_icon(\n                icon_kind,\n                cx,\n                45,\n                item_color,\n            )\n\n            canvas.create_text(\n                cx,\n                69,\n                text=label,\n                fill=self.INK if active else item_color,\n                font=(\n                    Fonts.FAMILY,\n                    9,\n                    "bold" if active else "normal",\n                ),\n                anchor="center",\n            )\n\n            if active:\n                canvas.create_line(\n                    cx - 16, 82,\n                    cx + 16, 82,\n                    fill=item_color,\n                    width=2,\n                )\n            else:\n                canvas.create_oval(\n                    cx - 1.5, 81,\n                    cx + 1.5, 84,\n                    fill=item_color,\n                    outline="",\n                )\n\n            add_click_region(\n                cx - width / 2,\n                cx + width / 2,\n                command,\n                enabled,\n            )\n\n        def redraw(_event=None) -> None:\n            width = max(1, int(canvas.winfo_width()))\n            height = max(1, int(canvas.winfo_height()))\n\n            if width <= 2 or height <= 2:\n                return\n\n            canvas.delete("all")\n            ribbon._nav_click_regions = []\n\n            background = build_background(width, height)\n            if background is not None:\n                try:\n                    from PIL import ImageTk\n\n                    photo = ImageTk.PhotoImage(background)\n                    ribbon._nav_background_photo = photo\n                    canvas.create_image(\n                        0,\n                        0,\n                        image=photo,\n                        anchor="nw",\n                    )\n                except Exception:\n                    pass\n\n            # --------------------------------------------------\n            # Titre de la page intégré au bandeau\n            # --------------------------------------------------\n\n            project_name = str(\n                getattr(self.project, "name", "")\n                or "Projet sans nom"\n            )\n            project_type = self._project_type_key()\n            appearance = self.PROJECT_TYPE_APPEARANCES.get(\n                project_type,\n                self.PROJECT_TYPE_APPEARANCES["ouvrage_structure"],\n            )\n\n            canvas.create_text(\n                20,\n                10,\n                text="Centre du projet",\n                fill=self.INK,\n                font=(Fonts.FAMILY, 11, "bold"),\n                anchor="nw",\n            )\n\n            canvas.create_text(\n                width - 20,\n                11,\n                text=f"{project_name}  ·  {appearance[\'label\']}",\n                fill=self.TEXT_MUTED,\n                font=(Fonts.FAMILY, 8),\n                anchor="ne",\n            )\n\n            # --------------------------------------------------\n            # Gauche : accès permanents\n            # --------------------------------------------------\n\n            draw_item(\n                cx=78,\n                icon_kind="eye",\n                label="Visualisation",\n                color=self.LILAC,\n                enabled=False,\n                width=104,\n            )\n\n            draw_item(\n                cx=190,\n                icon_kind="sprout",\n                label="Suivi du livre",\n                color=self.CELADON,\n                enabled=False,\n                width=106,\n            )\n\n            # Séparation discrète avant le parcours.\n            canvas.create_line(\n                252, 30,\n                252, 78,\n                fill="#D7DEE4",\n                width=1,\n            )\n\n            # --------------------------------------------------\n            # Centre : Accueil puis parcours\n            # --------------------------------------------------\n\n            steps = (\n                (\n                    "door",\n                    "Accueil",\n                    self.NAVY,\n                    self._return_home,\n                    False,\n                    True,\n                    72,\n                ),\n                (\n                    "home",\n                    "Centre",\n                    self.NAVY,\n                    None,\n                    True,\n                    True,\n                    72,\n                ),\n                (\n                    "pencil",\n                    "Maquettage",\n                    self.MAQUETTAGE,\n                    self._open_mockup,\n                    False,\n                    True,\n                    92,\n                ),\n                (\n                    "tools",\n                    "Atelier",\n                    self.ATELIER,\n                    self._open_model_workshop,\n                    False,\n                    True,\n                    76,\n                ),\n                (\n                    "quill",\n                    "Conception",\n                    self.CONCEPTION,\n                    self._open_atelier,\n                    False,\n                    True,\n                    92,\n                ),\n                (\n                    "puzzle",\n                    "Assemblage",\n                    self.ASSEMBLAGE,\n                    None,\n                    False,\n                    False,\n                    94,\n                ),\n                (\n                    "verify",\n                    "Vérification",\n                    self.VERIFICATION,\n                    None,\n                    False,\n                    False,\n                    94,\n                ),\n                (\n                    "flag",\n                    "Finalisation",\n                    self.FINALISATION,\n                    None,\n                    False,\n                    False,\n                    90,\n                ),\n            )\n\n            total_width = sum(step[6] for step in steps)\n            total_width += max(0, len(steps) - 1) * 2\n\n            available_left = 278\n            available_right = width - 100\n            available_width = max(\n                total_width,\n                available_right - available_left,\n            )\n            flow_left = (\n                available_left\n                + max(0, (available_width - total_width) / 2)\n            )\n\n            cursor = flow_left\n\n            for step in steps:\n                (\n                    icon_kind,\n                    label,\n                    color,\n                    command,\n                    active,\n                    enabled,\n                    item_width,\n                ) = step\n\n                cx = cursor + item_width / 2\n\n                draw_item(\n                    cx=cx,\n                    icon_kind=icon_kind,\n                    label=label,\n                    color=color,\n                    command=command,\n                    active=active,\n                    enabled=enabled,\n                    width=item_width,\n                )\n\n                cursor += item_width + 2\n\n            # --------------------------------------------------\n            # Droite : Fermer\n            # --------------------------------------------------\n\n            canvas.create_line(\n                width - 88, 30,\n                width - 88, 78,\n                fill="#D7DEE4",\n                width=1,\n            )\n\n            draw_item(\n                cx=width - 43,\n                icon_kind="close",\n                label="Fermer",\n                color=self.CORAL,\n                command=self._return_home,\n                enabled=True,\n                width=76,\n            )\n\n        canvas.bind(\n            "<Configure>",\n            redraw,\n            add="+",\n        )\n        canvas.after_idle(redraw)\n\n        return ribbon\n'
MARKER = "V6 :"


def fail(message: str) -> None:
    raise SystemExit(
        f"ERREUR : {message}\nAucun fichier n'a été modifié."
    )


def get_method(source: str, name: str) -> str:
    lines = source.splitlines(keepends=True)
    start = None

    for index, line in enumerate(lines):
        if line.startswith(f"    def {name}("):
            start = index
            break

    if start is None:
        fail(f"méthode {name} introuvable")

    end = len(lines)

    for index in range(start + 1, len(lines)):
        if (
            lines[index].startswith("    def ")
            or lines[index].startswith("    @")
        ):
            end = index
            break

    return "".join(lines[start:end])


def replace_method(
    source: str,
    name: str,
    replacement: str,
) -> str:
    old = get_method(source, name)

    return source.replace(
        old,
        replacement.rstrip() + "\n\n",
        1,
    )


def main() -> None:
    if not TARGET.is_file():
        fail(f"fichier introuvable : {TARGET}")

    original = TARGET.read_text(encoding="utf-8")
    current_nav = get_method(
        original,
        "_create_internal_navigation_ribbon",
    )

    if MARKER in current_nav:
        print("BANDEAU_DECORATIF_V6_DEJA_APPLIQUE")
        return

    candidate = replace_method(
        original,
        "show",
        NEW_SHOW,
    )

    candidate = replace_method(
        candidate,
        "_create_internal_navigation_ribbon",
        NEW_NAV,
    )

    try:
        compile(candidate, str(TARGET), "exec")
    except Exception as exc:
        fail(f"la nouvelle version ne compile pas : {exc}")

    backup_dir = PROJECT / "cache" / "correctifs"
    backup_dir.mkdir(parents=True, exist_ok=True)

    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup = (
        backup_dir
        / f"document_view_avant_bandeau_v6_{stamp}.py"
    )
    temp = TARGET.with_suffix(".bandeau_v6.tmp")

    try:
        temp.write_text(candidate, encoding="utf-8")
        py_compile.compile(str(temp), doraise=True)

        shutil.copy2(TARGET, backup)
        temp.replace(TARGET)

        py_compile.compile(str(TARGET), doraise=True)

    except Exception as exc:
        try:
            temp.unlink(missing_ok=True)
        except Exception:
            pass

        if backup.exists():
            shutil.copy2(backup, TARGET)

        fail(
            f"installation annulée automatiquement : {exc}"
        )

    print("BANDEAU_DECORATIF_V6_OK")
    print("Les icônes sont dessinées directement sur le décor : plus de carrés gris.")
    print("Le fond général PageMaître est visible dans le ruban.")
    print("Le titre Centre du projet est intégré au bandeau.")
    print("L'ancien en-tête séparé est supprimé de l'affichage.")
    print("Accueil reste juste avant Centre.")
    print(f"Sauvegarde : {backup}")


if __name__ == "__main__":
    main()
