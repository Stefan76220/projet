from __future__ import annotations

from datetime import datetime
from pathlib import Path
import py_compile
import shutil

PROJECT = Path(r"C:\Users\PC\projet")
TARGET = PROJECT / "src" / "gui" / "views" / "mockup_view.py"
MARKER = "PRECHEMIN_ERGONOMIE_COMPACTE_V1"
BLOCKS = {'_create_sequence_panel': '    def _create_sequence_panel(self, parent) -> ctk.CTkFrame:\n        # PRECHEMIN_ERGONOMIE_COMPACTE_V1\n        """Pré-chemin de fer compact : contenu regroupé à gauche, façon atelier."""\n        panel = ctk.CTkFrame(\n            parent,\n            fg_color=self.CARD_BG,\n            corner_radius=8,\n            border_width=1,\n            border_color=self.BORDER,\n        )\n        panel.grid_columnconfigure(0, weight=1)\n        panel.grid_rowconfigure(2, weight=1)\n\n        title_row = ctk.CTkFrame(\n            panel,\n            width=900,\n            height=34,\n            fg_color="transparent",\n        )\n        title_row.grid(row=0, column=0, sticky="w", padx=10, pady=(4, 3))\n        title_row.grid_columnconfigure(0, weight=1)\n        title_row.grid_propagate(False)\n\n        ctk.CTkLabel(\n            title_row,\n            text="Pré-chemin de fer",\n            font=Fonts.H2,\n            text_color=self.INK,\n        ).grid(row=0, column=0, sticky="w")\n\n        self._progress_label = ctk.CTkLabel(\n            title_row,\n            text="",\n            font=Fonts.SMALL,\n            text_color=self.DONE,\n        )\n        self._progress_label.grid(row=0, column=1, sticky="e", padx=(8, 10))\n\n        self._summary_label = ctk.CTkLabel(\n            title_row,\n            text="",\n            font=Fonts.SMALL,\n            text_color=self.INK,\n        )\n        self._summary_label.grid(row=0, column=2, sticky="e")\n\n        self._selection_bar = ctk.CTkFrame(\n            panel,\n            width=590,\n            height=32,\n            fg_color="#EAF1F7",\n            corner_radius=6,\n            border_width=1,\n            border_color="#C9D8E6",\n        )\n        self._selection_bar.grid(\n            row=1,\n            column=0,\n            sticky="w",\n            padx=10,\n            pady=(0, 6),\n        )\n        self._selection_bar.grid_propagate(False)\n\n        self._selection_label = ctk.CTkLabel(\n            self._selection_bar,\n            text="",\n            width=230,\n            font=Fonts.SMALL,\n            text_color=self.INK,\n            anchor="w",\n        )\n        self._selection_label.grid(\n            row=0,\n            column=0,\n            sticky="w",\n            padx=(9, 5),\n            pady=3,\n        )\n\n        self._selection_duplicate_button = ctk.CTkButton(\n            self._selection_bar,\n            text="⧉ Dupliquer",\n            width=92,\n            height=24,\n            corner_radius=5,\n            fg_color=self.GROUP_BG,\n            hover_color=self.ACCENT_SOFT,\n            text_color=self.INK,\n            border_width=1,\n            border_color=self.BORDER,\n            font=Fonts.SMALL,\n            command=self._duplicate_selected_pages,\n        )\n        self._selection_duplicate_button.grid(\n            row=0,\n            column=1,\n            padx=2,\n            pady=3,\n        )\n\n        self._selection_delete_button = ctk.CTkButton(\n            self._selection_bar,\n            text="✕ Supprimer",\n            width=94,\n            height=24,\n            corner_radius=5,\n            fg_color=self.GROUP_BG,\n            hover_color="#F3E4E1",\n            text_color=self.DANGER,\n            border_width=1,\n            border_color="#E0B9B0",\n            font=Fonts.SMALL,\n            command=self._delete_selected_pages,\n        )\n        self._selection_delete_button.grid(\n            row=0,\n            column=2,\n            padx=2,\n            pady=3,\n        )\n\n        ctk.CTkButton(\n            self._selection_bar,\n            text="×",\n            width=26,\n            height=24,\n            corner_radius=5,\n            fg_color=self.GROUP_BG,\n            hover_color=Colors.BUTTON_HOVER,\n            text_color=self.TEXT_MUTED,\n            border_width=1,\n            border_color=self.BORDER,\n            font=Fonts.SMALL,\n            command=self._clear_page_selection,\n        ).grid(row=0, column=3, padx=(2, 5), pady=3)\n\n        self._selection_label.configure(\n            text="Sélection : clic · Ctrl/Maj pour ajouter"\n        )\n        self._selection_duplicate_button.configure(state="disabled")\n        self._selection_delete_button.configure(state="disabled")\n\n        self._sequence_frame = ctk.CTkScrollableFrame(\n            panel,\n            width=900,\n            fg_color=self.RIBBON_BG,\n            corner_radius=6,\n        )\n        self._sequence_frame.grid(\n            row=2,\n            column=0,\n            sticky="nsw",\n            padx=10,\n            pady=(0, 8),\n        )\n        self._sequence_frame.grid_columnconfigure(0, weight=1)\n\n        return panel', '_refresh_sequence': '    def _refresh_sequence(self) -> None:\n        """Synchronise les cartes compactes sans reconstruire tout le bureau."""\n        self._hide_page_drop_indicator()\n        if self._sequence_frame is None:\n            return\n\n        items = self._items()\n        active_ids = {str(item.get("id", "")) for item in items}\n        self._sanitize_page_selection(active_ids)\n\n        for item_id in list(self._sequence_row_widgets):\n            if item_id in active_ids:\n                continue\n            record = self._sequence_row_widgets.pop(item_id)\n            self._sequence_row_signatures.pop(item_id, None)\n            self._rendered_selected_page_ids.discard(item_id)\n            try:\n                record["row"].destroy()\n            except Exception:\n                pass\n\n        if not items:\n            if self._sequence_empty_label is None:\n                self._sequence_empty_label = ctk.CTkLabel(\n                    self._sequence_frame,\n                    text="Clique sur une page pour commencer.",\n                    font=Fonts.NORMAL,\n                    text_color=self.TEXT_LIGHT,\n                )\n            self._sequence_empty_label.grid(\n                row=0,\n                column=0,\n                sticky="w",\n                padx=20,\n                pady=28,\n            )\n        else:\n            if self._sequence_empty_label is not None:\n                self._sequence_empty_label.grid_remove()\n\n            total = len(items)\n            for index, item in enumerate(items):\n                item_id = str(item.get("id", ""))\n                automatic_blank = bool(item.get("automatic_recto_verso", False))\n                row_padx = (28, 4) if automatic_blank else (4, 4)\n\n                if item_id not in self._sequence_row_widgets:\n                    row = self._create_sequence_row(\n                        self._sequence_frame,\n                        item,\n                        index,\n                        total,\n                    )\n                    row.grid(\n                        row=index,\n                        column=0,\n                        sticky="w",\n                        padx=row_padx,\n                        pady=3,\n                    )\n                    self._sequence_row_widgets[item_id]["grid_index"] = index\n                    self._sequence_row_widgets[item_id]["grid_padx"] = row_padx\n                else:\n                    signature = self._sequence_row_signature(\n                        item,\n                        index,\n                        total,\n                    )\n                    if self._sequence_row_signatures.get(item_id) != signature:\n                        self._update_sequence_row(item, index, total)\n\n                    record = self._sequence_row_widgets[item_id]\n                    if (\n                        record.get("grid_index") != index\n                        or record.get("grid_padx") != row_padx\n                    ):\n                        record["row"].grid_configure(\n                            row=index,\n                            column=0,\n                            sticky="w",\n                            padx=row_padx,\n                            pady=3,\n                        )\n                        record["grid_index"] = index\n                        record["grid_padx"] = row_padx\n\n        self._rendered_selected_page_ids = set(self._selected_page_ids)\n        self._update_summary()\n        self._update_page_type_button_states()\n        self._update_selection_controls()', '_create_sequence_row': '    def _create_sequence_row(\n        self,\n        parent,\n        item: dict[str, Any],\n        index: int,\n        total_items: int,\n    ) -> ctk.CTkFrame:\n        item_id = str(item.get("id", ""))\n        definition = self._definition_for(item.get("type", "inconnu"))\n        accent = str(definition.get("accent", self.INK))\n\n        row = ctk.CTkFrame(\n            parent,\n            width=840,\n            height=66,\n            fg_color=self.CARD_BG,\n            corner_radius=7,\n            border_width=1,\n            border_color=self.BORDER,\n        )\n        row.grid_propagate(False)\n\n        thumbnail = ctk.CTkFrame(\n            row,\n            width=42,\n            height=54,\n            fg_color=str(definition.get("color", self.GROUP_BG)),\n            corner_radius=5,\n            border_width=1,\n            border_color=accent,\n        )\n        thumbnail.grid(row=0, column=0, padx=(6, 8), pady=5)\n        thumbnail.grid_propagate(False)\n\n        symbol_label = ctk.CTkLabel(\n            thumbnail,\n            text=str(definition.get("symbol", "?")),\n            font=(Fonts.FAMILY, 14, "bold"),\n            text_color=accent,\n        )\n        symbol_label.place(relx=0.5, rely=0.34, anchor="center")\n\n        count_badge = ctk.CTkLabel(\n            thumbnail,\n            text="1",\n            font=(Fonts.FAMILY, 10, "bold"),\n            text_color=self.INK,\n        )\n        count_badge.place(relx=0.5, rely=0.78, anchor="center")\n\n        info = ctk.CTkFrame(\n            row,\n            width=315,\n            height=56,\n            fg_color="transparent",\n            corner_radius=0,\n        )\n        info.grid(row=0, column=1, sticky="w", pady=4)\n        info.grid_propagate(False)\n        info.grid_columnconfigure(0, weight=1)\n\n        title_label = ctk.CTkLabel(\n            info,\n            text="",\n            width=300,\n            font=Fonts.NORMAL,\n            text_color=self.INK,\n            anchor="w",\n        )\n        title_label.grid(row=0, column=0, sticky="w", pady=(5, 0))\n\n        detail_label = ctk.CTkLabel(\n            info,\n            text="",\n            width=300,\n            font=Fonts.SMALL,\n            text_color=self.TEXT_MUTED,\n            anchor="w",\n        )\n        detail_label.grid(row=1, column=0, sticky="w", pady=(0, 4))\n\n        actions = ctk.CTkFrame(\n            row,\n            height=36,\n            fg_color=self.RIBBON_BG,\n            corner_radius=6,\n            border_width=1,\n            border_color=self.BORDER,\n        )\n        actions.grid(row=0, column=2, sticky="w", padx=(4, 0), pady=13)\n        actions.grid_propagate(False)\n\n        up_button = self._small_button(\n            actions,\n            "↑",\n            lambda current=item_id: self._move_item_by_id(current, -1),\n        )\n        up_button.grid(row=0, column=0, padx=(4, 1), pady=5)\n\n        down_button = self._small_button(\n            actions,\n            "↓",\n            lambda current=item_id: self._move_item_by_id(current, 1),\n        )\n        down_button.grid(row=0, column=1, padx=1, pady=5)\n\n        separator_1 = ctk.CTkFrame(\n            actions,\n            width=1,\n            height=22,\n            fg_color=self.BORDER,\n            corner_radius=0,\n        )\n        separator_1.grid(row=0, column=2, padx=5, pady=5)\n\n        minus_button = self._small_button(\n            actions,\n            "−",\n            lambda current=item_id: self._change_count_by_id(current, -1),\n        )\n        minus_button.grid(row=0, column=3, padx=1, pady=5)\n\n        count_label = ctk.CTkLabel(\n            actions,\n            text="1",\n            width=28,\n            font=(Fonts.FAMILY, 10, "bold"),\n            text_color=self.INK,\n        )\n        count_label.grid(row=0, column=4, padx=1)\n\n        plus_button = self._small_button(\n            actions,\n            "+",\n            lambda current=item_id: self._change_count_by_id(current, 1),\n        )\n        plus_button.grid(row=0, column=5, padx=1, pady=5)\n\n        separator_2 = ctk.CTkFrame(\n            actions,\n            width=1,\n            height=22,\n            fg_color=self.BORDER,\n            corner_radius=0,\n        )\n        separator_2.grid(row=0, column=6, padx=5, pady=5)\n\n        delete_button = ctk.CTkButton(\n            actions,\n            text="Supprimer",\n            width=72,\n            height=24,\n            corner_radius=5,\n            fg_color=self.GROUP_BG,\n            hover_color="#F3E4E1",\n            text_color=self.DANGER,\n            border_width=1,\n            border_color="#E0B9B0",\n            font=Fonts.SMALL,\n            command=lambda current=item_id: self._remove_item_by_id(current),\n        )\n        delete_button.grid(row=0, column=7, padx=(1, 5), pady=5)\n\n        self._sequence_row_widgets[item_id] = {\n            "row": row,\n            "thumbnail": thumbnail,\n            "symbol_label": symbol_label,\n            "count_badge": count_badge,\n            "title_label": title_label,\n            "detail_label": detail_label,\n            "up_button": up_button,\n            "down_button": down_button,\n            "minus_button": minus_button,\n            "count_label": count_label,\n            "plus_button": plus_button,\n            "delete_button": delete_button,\n            "grid_index": index,\n            "grid_padx": (4, 4),\n        }\n\n        self._bind_sequence_page_drag(\n            item_id,\n            row,\n            thumbnail,\n            symbol_label,\n            count_badge,\n            info,\n            title_label,\n            detail_label,\n        )\n        self._update_sequence_row(item, index, total_items)\n        return row\n\n    def _item_progress_counts(\n        self,\n        item: dict[str, Any],\n    ) -> tuple[int, int]:\n        """Retourne (fait, reste), prêt pour la future liaison Conception."""\n        total = max(1, int(item.get("count", 1)))\n        completed = None\n        for key in (\n            "done_count",\n            "validated_count",\n            "completed_count",\n        ):\n            if key not in item:\n                continue\n            try:\n                completed = int(item.get(key, 0))\n            except (TypeError, ValueError):\n                completed = 0\n            break\n\n        if completed is None:\n            completed = total if bool(item.get("done", False)) else 0\n\n        completed = max(0, min(total, completed))\n        return completed, total - completed\n\n    def _associated_automatic_blank_count(self, item_id: str) -> int:\n        total = 0\n        for candidate in self._items():\n            if not bool(candidate.get("automatic_recto_verso", False)):\n                continue\n            if str(candidate.get("recto_target_id", "")) != item_id:\n                continue\n            total += max(1, int(candidate.get("count", 1)))\n        return total\n\n    def _automatic_blank_target_title(self, item: dict[str, Any]) -> str:\n        target_id = str(item.get("recto_target_id", ""))\n        if not target_id:\n            return ""\n        for candidate in self._items():\n            if str(candidate.get("id", "")) != target_id:\n                continue\n            definition = self._definition_for(\n                str(candidate.get("type", "inconnu"))\n            )\n            return str(\n                candidate.get("title")\n                or definition.get("title", "")\n            )\n        return ""', '_update_sequence_row': '    def _update_sequence_row(\n        self,\n        item: dict[str, Any],\n        index: int,\n        total_items: int,\n    ) -> None:\n        item_id = str(item.get("id", ""))\n        record = self._sequence_row_widgets.get(item_id)\n        if record is None:\n            return\n\n        definition = self._definition_for(item.get("type", "inconnu"))\n        count = max(1, int(item.get("count", 1)))\n        completed, remaining = self._item_progress_counts(item)\n        done = remaining == 0\n        single = bool(definition.get("single", False))\n        required = bool(definition.get("required", False))\n        automatic_blank = bool(item.get("automatic_recto_verso", False))\n        accent = str(definition.get("accent", self.INK))\n\n        self._update_sequence_row_selection(item)\n        record["thumbnail"].configure(\n            fg_color=str(definition.get("color", self.GROUP_BG)),\n            border_color=accent,\n        )\n        record["symbol_label"].configure(\n            text=str(definition.get("symbol", "?")),\n            text_color=accent,\n        )\n        record["count_badge"].configure(\n            text=f"×{count}" if count > 1 else "1",\n        )\n\n        title_text = str(item.get("title") or definition.get("title", "Page"))\n        if automatic_blank:\n            position_text = (\n                "avant" if item.get("recto_position") == "before" else "après"\n            )\n            title_text = (\n                "Pages blanches automatiques"\n                if count > 1\n                else "Page blanche automatique"\n            )\n            target_title = self._automatic_blank_target_title(item)\n            detail_text = (\n                f"Auto recto-verso · {count} générée"\n                f"{\'s\' if count > 1 else \'\'} · {position_text}"\n            )\n            if target_title:\n                detail_text += f" · liée à {target_title}"\n            if bool(item.get("recto_shared", False)):\n                detail_text += " · partagé"\n        else:\n            auto_count = self._associated_automatic_blank_count(item_id)\n            if count > 1:\n                detail_text = f"Fait {completed}/{count} · Reste {remaining}"\n            else:\n                detail_text = "Fait" if done else "À faire"\n\n            if auto_count:\n                detail_text += (\n                    f" · Auto associée : {auto_count}"\n                    if auto_count == 1\n                    else f" · Auto associées : {auto_count}"\n                )\n\n        record["title_label"].configure(\n            text=title_text,\n            text_color=self.TEXT_MUTED if automatic_blank else (\n                self.DONE if done else self.INK\n            ),\n        )\n        record["detail_label"].configure(\n            text=detail_text,\n            text_color=(\n                self.TEXT_MUTED\n                if automatic_blank\n                else self.DONE if done else self.TEXT_MUTED\n            ),\n        )\n        record["count_label"].configure(text=str(count))\n\n        record["up_button"].configure(\n            state="normal" if self._can_move_item(index, -1) else "disabled",\n        )\n        record["down_button"].configure(\n            state="normal" if self._can_move_item(index, 1) else "disabled",\n        )\n        record["minus_button"].configure(\n            state=(\n                "disabled"\n                if automatic_blank or single or count <= 1\n                else "normal"\n            ),\n        )\n        record["plus_button"].configure(\n            state="disabled" if automatic_blank or single else "normal",\n        )\n        record["delete_button"].configure(\n            state="disabled" if automatic_blank or required else "normal",\n        )\n\n        self._sequence_row_signatures[item_id] = self._sequence_row_signature(\n            item,\n            index,\n            total_items,\n        )', '_sequence_row_signature': '    def _sequence_row_signature(\n        self,\n        item: dict[str, Any],\n        index: int,\n        total_items: int,\n    ) -> tuple[Any, ...]:\n        """État utile d\'une carte, progression et liens automatiques compris."""\n        definition = self._definition_for(item.get("type", "inconnu"))\n        item_id = str(item.get("id", ""))\n        completed, remaining = self._item_progress_counts(item)\n        return (\n            str(item.get("type", "")),\n            str(item.get("title", "")),\n            max(1, int(item.get("count", 1))),\n            completed,\n            remaining,\n            bool(item.get("done", False)),\n            str(item.get("done_source", "")),\n            bool(item.get("automatic_recto_verso", False)),\n            str(item.get("recto_position", "")),\n            str(item.get("recto_target_id", "")),\n            bool(item.get("recto_shared", False)),\n            self._associated_automatic_blank_count(item_id),\n            str(definition.get("title", "")),\n            str(definition.get("symbol", "")),\n            str(definition.get("color", "")),\n            str(definition.get("accent", "")),\n            bool(definition.get("single", False)),\n            bool(definition.get("required", False)),\n            index,\n            total_items,\n        )', '_update_selection_controls': '    def _update_selection_controls(self) -> None:\n        if self._selection_bar is None:\n            return\n\n        count = len(self._selected_page_ids)\n        if count == 0:\n            if self._selection_label is not None:\n                self._selection_label.configure(\n                    text="Sélection : clic · Ctrl/Maj pour ajouter"\n                )\n            if self._selection_duplicate_button is not None:\n                self._selection_duplicate_button.configure(state="disabled")\n            if self._selection_delete_button is not None:\n                self._selection_delete_button.configure(state="disabled")\n            return\n\n        duplicable_count = len(self._selected_duplicable_items())\n        deletable_count = len(self._selected_deletable_ids())\n\n        if self._selection_label is not None:\n            self._selection_label.configure(\n                text=(\n                    f"{count} page{\'s\' if count != 1 else \'\'} "\n                    f"sélectionnée{\'s\' if count != 1 else \'\'}"\n                )\n            )\n        if self._selection_duplicate_button is not None:\n            self._selection_duplicate_button.configure(\n                state="normal" if duplicable_count else "disabled"\n            )\n        if self._selection_delete_button is not None:\n            self._selection_delete_button.configure(\n                state="normal" if deletable_count else "disabled"\n            )', '_update_summary': '    def _update_summary(self) -> None:\n        items = self._items()\n        total_pages = sum(\n            max(1, int(item.get("count", 1)))\n            for item in items\n        )\n        automatic_pages = sum(\n            max(1, int(item.get("count", 1)))\n            for item in items\n            if bool(item.get("automatic_recto_verso", False))\n        )\n\n        work_items = [\n            item\n            for item in items\n            if not bool(item.get("automatic_recto_verso", False))\n        ]\n        work_total = sum(\n            max(1, int(item.get("count", 1)))\n            for item in work_items\n        )\n        done_pages = sum(\n            self._item_progress_counts(item)[0]\n            for item in work_items\n        )\n        distinct_types = len({\n            str(item.get("type", "autre"))\n            for item in items\n        })\n\n        if self._summary_label is not None:\n            auto_text = f" · {automatic_pages} auto" if automatic_pages else ""\n            self._summary_label.configure(\n                text=(\n                    f"{total_pages} p.{auto_text} · "\n                    f"{distinct_types} type{\'s\' if distinct_types != 1 else \'\'}"\n                )\n            )\n\n        if self._progress_label is not None:\n            self._progress_label.configure(\n                text=f"Fait : {done_pages}/{work_total}" if work_total else ""\n            )'}


def fail(message: str) -> None:
    raise SystemExit(f"ERREUR : {message}\nAucun fichier n'a été modifié.")


def replace_method(source: str, name: str, new_block: str) -> str:
    lines = source.splitlines(keepends=True)
    start = None
    for index, line in enumerate(lines):
        if line.startswith(f"    def {name}("):
            start = index
            break
    if start is None:
        fail(f"méthode {name} introuvable")

    end = len(lines)
    for index in range(start + 1, len(lines)):
        line = lines[index]
        if line.startswith("    def ") or line.startswith("    @"):
            end = index
            break

    return (
        "".join(lines[:start])
        + new_block.rstrip()
        + "\n\n"
        + "".join(lines[end:])
    )


def main() -> None:
    if not TARGET.is_file():
        fail(f"fichier introuvable : {TARGET}")

    original = TARGET.read_text(encoding="utf-8")
    if MARKER in original:
        print("PRECHEMIN_ERGONOMIE_DEJA_APPLIQUEE")
        return

    source = original
    for name in (
        "_create_sequence_panel",
        "_refresh_sequence",
        "_create_sequence_row",
        "_update_sequence_row",
        "_sequence_row_signature",
        "_update_selection_controls",
        "_update_summary",
    ):
        source = replace_method(source, name, BLOCKS[name])

    try:
        compile(source, str(TARGET), "exec")
    except Exception as exc:
        fail(f"le code produit ne compile pas : {exc}")

    backup_dir = PROJECT / "cache" / "correctifs"
    backup_dir.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup = backup_dir / f"mockup_view_avant_ergonomie_prechemin_{stamp}.py"
    shutil.copy2(TARGET, backup)

    try:
        TARGET.write_text(source, encoding="utf-8")
        py_compile.compile(str(TARGET), doraise=True)
    except Exception as exc:
        shutil.copy2(backup, TARGET)
        fail(f"correction annulée automatiquement : {exc}")

    pycache = TARGET.parent / "__pycache__"
    if pycache.exists():
        shutil.rmtree(pycache, ignore_errors=True)

    print("PRECHEMIN_ERGONOMIE_COMPACTE_OK")
    print("Fichier modifié : src/gui/views/mockup_view.py")
    print(f"Sauvegarde : {backup}")
    print("Cartes compactes, commandes rapprochées, progression et blancs auto affichés.")
    print("Ferme complètement PageMaître puis relance-le.")


if __name__ == "__main__":
    main()
